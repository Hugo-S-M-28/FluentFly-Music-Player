<Window
    x:Name="TaskbarWindowRoot"
    x:Class="FluentFlyout.Windows.TaskbarWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:FluentFlyout.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
    xmlns:local="clr-namespace:FluentFlyout.Windows"
    mc:Ignorable="d"
    Title="TaskbarWindow"
    Height="40"
    Width="100"
    WindowStyle="None"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    AllowsTransparency="True"
    Background="Transparent"
    TextOptions.TextRenderingMode="ClearType"
    TextOptions.TextFormattingMode="Ideal"
    FontFamily="{Binding Settings.FontFamily}"
    FlowDirection="{Binding Settings.FlowDirection}"
    Loaded="Window_Loaded"
    Visibility="Collapsed"
    d:Visibility="Visible"
    d:DataContext="{d:DesignInstance viewModels:SettingsShellViewModel}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/FluentFlyout;component/Resources/SharedThemeResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Canvas
        Name="WidgetCanvas"
        Background="Transparent">
        <controls:TaskbarVisualizerControl
            x:Name="TaskbarVisualizer"
            Margin="0,1,0,0"
            BackdropImage="{Binding NowPlaying.BackdropImage, ElementName=TaskbarWindowRoot}"/>
        <controls:TaskbarWidgetControl
            x:Name="Widget"
            BackdropImage="{Binding NowPlaying.BackdropImage, ElementName=TaskbarWindowRoot}"/>
    </Canvas>
</Window>
