<Page x:Class="FluentFlyoutWPF.Pages.LockKeysPage"
      x:Name="PageRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      mc:Ignorable="d"
      d:DesignHeight="800"
      d:DesignWidth="800"
      Title="LockKeysPage"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Converters.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <ScrollViewer x:Name="PageScrollViewer"
                      VerticalScrollBarVisibility="Visible"
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False"
                      PanningMode="VerticalOnly"
                      Padding="0,0,10,0">
            <StackPanel DataContext="{Binding Settings}"
                        MaxWidth="{Binding ElementName=PageRoot, Path=ActualWidth, Converter={StaticResource ResponsivePageContentMaxWidthConverter}}"
                        Margin="{Binding ElementName=PageRoot, Path=ActualWidth, Converter={StaticResource ResponsivePageMarginConverter}}">
                <ui:TextBlock Text="{DynamicResource LockKeysCustomizationTitle}"
                              FontTypography="Title"
                              Margin="0,0,0,20"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <WrapPanel Margin="0,0,0,24">
                        <Border CornerRadius="10"
                                ClipToBounds="True"
                                Width="164"
                                Height="98"
                                Margin="0,0,12,12">
                            <Border.Background>
                                <ImageBrush ImageSource="/Resources/FluentFlyoutDemo5.1.png"
                                            Stretch="UniformToFill"/>
                            </Border.Background>
                        </Border>
                        <TextBlock FontSize="14"
                                   FontWeight="Regular"
                                   HorizontalAlignment="Stretch"
                                   Opacity="0.75"
                                   Margin="0"
                                   MaxWidth="{Binding ElementName=PageRoot, Path=ActualWidth, Converter={StaticResource ResponsiveBannerTextWidthConverter}}"
                                   TextWrapping="WrapWithOverflow"
                                   Text="{DynamicResource LockKeysDescription}"/>
                    </WrapPanel>
                    <ui:CardControl Grid.Row="1"
                                    Icon="{ui:SymbolIcon LockMultiple24}"
                                    Margin="0,0,0,24">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{DynamicResource EnableLockKeysTitle}"
                                           FontSize="14"
                                           FontWeight="Regular"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource EnableLockKeysDescription}"
                                           FontSize="12"
                                           Opacity="0.5"/>
                            </StackPanel>
                        </ui:CardControl.Header>
                        <controls:ToggleSwitch Name="LockKeysSwitch"
                                               IsChecked="{Binding LockKeysEnabled, Mode=TwoWay}"/>
                    </ui:CardControl>
                    <ui:CardControl Grid.Row="2"
                                    Icon="{ui:SymbolIcon Blur24}"
                                    Margin="0,0,0,3">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{DynamicResource AcrylicWindowTitle}"
                                           FontSize="14"
                                           FontWeight="Regular"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource AcrylicWindowDescription}"
                                           FontSize="12"
                                           Opacity="0.5"/>
                            </StackPanel>
                        </ui:CardControl.Header>
                        <controls:ToggleSwitch Name="LockKeysAcrylicWindowSwitch"
                                               IsChecked="{Binding LockKeysAcrylicWindowEnabled, Mode=TwoWay}"/>
                    </ui:CardControl>
                    <ui:CardControl Grid.Row="3"
                                    Icon="{ui:SymbolIcon Timer24}"
                                    Margin="0,0,0,3">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{DynamicResource LockKeysStayDurationTitle}"
                                           FontSize="14"
                                           FontWeight="Regular"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource LockKeysStayDurationDescription}"
                                           FontSize="12"
                                           Opacity="0.5"/>
                            </StackPanel>
                        </ui:CardControl.Header>
                        <StackPanel Orientation="Horizontal">
                            <ui:TextBox Width="60"
                                        ClearButtonEnabled="False"
                                        Text="{Binding LockKeysDurationText, Mode=TwoWay}"/>
                            <TextBlock Text="{DynamicResource MillisecondsUnit}"
                                       FontSize="14"
                                       FontWeight="Regular"
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:CardControl>
                    <ui:CardControl Grid.Row="4"
                                    Icon="{ui:SymbolIcon TextBold24}"
                                    Margin="0,0,0,3">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{DynamicResource LockKeysBoldUITitle}"
                                           FontSize="14"
                                           FontWeight="Regular"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource LockKeysBoldUIDescription}"
                                           FontSize="12"
                                           Opacity="0.5"/>
                            </StackPanel>
                        </ui:CardControl.Header>
                        <controls:ToggleSwitch Name="LockKeysBoldUISwitch"
                                               IsChecked="{Binding LockKeysBoldUi, Mode=TwoWay}"/>
                    </ui:CardControl>
                    <ui:CardControl Grid.Row="5"
                                    Icon="{ui:SymbolIcon Wand24}"
                                    Margin="0,0,0,3">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{DynamicResource LockKeysAnimatedTitle}"
                                           FontSize="14"
                                           FontWeight="Regular"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource LockKeysAnimatedDescription}"
                                           FontSize="12"
                                           Opacity="0.5"/>
                            </StackPanel>
                        </ui:CardControl.Header>
                        <controls:ToggleSwitch Name="LockKeysAnimatedSwitch"
                                               IsChecked="{Binding LockKeysAnimated, Mode=TwoWay}"/>
                    </ui:CardControl>
                    <ui:CardControl Grid.Row="6"
                                    Icon="{ui:SymbolIcon ProjectionScreen24}"
                                    Margin="0,0,0,3">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{DynamicResource LockKeysCursorUITitle}"
                                               FontSize="14"
                                               FontWeight="Regular"
                                               VerticalAlignment="Center"/>
                                    <ui:InfoBadge Value=" NEW "
                                                  FontWeight="Medium"
                                                  FontSize="10"
                                                  Margin="6,-2,0,-2"
                                                  Severity="Success"/>
                                </StackPanel>
                                <TextBlock Text="{DynamicResource LockKeysCursorUIDescription}"
                                           FontSize="12"
                                           Opacity="0.5"/>
                            </StackPanel>
                        </ui:CardControl.Header>
                        <ComboBox Name="LockKeysMonitorPreferenceComboBox"
                                  SelectedIndex="{Binding LockKeysMonitorPreference, Mode=TwoWay}"
                                  Width="{Binding ElementName=PageRoot, Path=ActualWidth, Converter={StaticResource ResponsiveInlineControlWidthConverter}, ConverterParameter=wide}">
                            <ComboBoxItem Content="{DynamicResource DefaultLockLayoutPosition}"/>
                            <ComboBoxItem Content="{DynamicResource MonitorWithFocusedWindow}"/>
                            <ComboBoxItem Content="{DynamicResource MonitorWithCursor}"/>
                        </ComboBox>
                    </ui:CardControl>
                    <ui:CardControl Grid.Row="7"
                                    Icon="{ui:SymbolIcon Info24}"
                                    Margin="0,0,0,76">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{DynamicResource EnableInsertKeyTitle}"
                                           FontSize="14"
                                           FontWeight="Regular"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{DynamicResource EnableInsertKeyDescription}"
                                           FontSize="12"
                                           Opacity="0.5"/>
                            </StackPanel>
                        </ui:CardControl.Header>
                        <controls:ToggleSwitch Name="LockKeysEnableInsertSwitch"
                                               IsChecked="{Binding LockKeysInsertEnabled, Mode=TwoWay}"/>
                    </ui:CardControl>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
    
